import { Cargo } from './cargo';

export class Directiva{
    idDirectiva: number;
    rut: string;
    nombre: string;
    paterno: string;
    materno: string;
    email: string;
    cargo: Cargo;
}